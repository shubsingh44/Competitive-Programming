#include <iostream>
#include<bits/stdc++.h>
using namespace std ;
int put()
{
    int n,m;
    cin>>n>>m;
    int arr[n+1];
    memset(arr,0,sizeof(arr));
    while(m--)
    {

    }
}
int main( ) {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin>>n;
    while(n--)
     put();
    return 0;
}
